<h1>Dr. <%= @doctor.name %></h1>

<h2>Specialty: <%= @doctor.specialty %></h2>
<h2>University: <%= @doctor.education %></h2>
<h2>Hospital: <%= @doctor.hospital.name %></h2><br>
<%= link_to "Assign Dr. #{@doctor.name} to a Different Hospital", "/doctors/#{@doctor.id}/edit" %>

<h2>Patients:</h2>
<% @doctor.patients.each do |patient| %>
<section id = "patient-<%= patient.id %>">
<ul>
  <%= patient.name %><br>
  <%= link_to "Remove This Patient from Dr. #{@doctor.name}'s Caseload", "/doctors/#{@doctor.id}/patients/#{patient.id}", method: :delete %>
</ul>
</section>
<% end %>
