<h1>Reassign Hospital for <%= @doctor.name %></h1>

<%= form_tag "/doctors/#{@doctor.id}/update_hospital", method: :patch do |f| %>

<%= label_tag :hospital_id %>
<%= text_field_tag "hospital_id" %><br>

<%= submit_tag "Add #{@doctor.name} to this hospital" %>
<% end %>